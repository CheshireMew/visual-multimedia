---
name: visual-multimedia
description: 把用户已经确认的主题、内容真源或现有素材转化为可修改、可预览、可导出的社交卡、封面、图表、GIF、代码动画、可手动推进的多场景 HTML、Live Photo、视频、音频或播客，并负责相应的标题、口播、字幕、节目结构和媒体配套帖子。Use when the user asks to 选择具体媒体载体、先写媒体文案再制作视觉、把卡片或动画做成可编辑网页、制作手动/自动/混合播放的 HTML 演示与动画、制作多比例封面或轮播图、导出图片、GIF 或 Live Photo、制作无实拍视频、从单张图或角色设定建立无需 Live2D 的二次元中文口播形象及可复用口型素材库、剪辑已有实拍/访谈/录屏素材，或编辑音频与播客；不用于 PPTX、从长材料中筛选什么值得分享、独立纯文字短帖与 Thread、主动研究或核查主题、撰写长文与 Newsletter、规划拍摄、社交媒体运营、上传或发布。
---

# Visual Multimedia

## 目标与边界

把已经成立的内容转化为合适的媒体表达，并保留可以继续修改的制作真源。Agent 负责载体判断、媒体文案、样稿、制作、修改、导出和真实结果检查；具体观点、事实、经历和来源仍由用户输入或上游内容真源决定。

适用于：

- 从确认的主张、资料、文章、脚本或内容包中选择静态图文、GIF、视频、音频或播客。
- 起草或修改标题、卡片文字、口播、字幕、节目结构和媒体配套帖子。
- 把图文卡片、封面、图表、动态图解、GIF 和代码生成画面先做成可编辑网页，再派生用户需要的媒体。
- 把多场景代码动画按用户推进、自动播放或混合播放；同一网页真源可以交互演示，也可以连续导出为动画或视频。
- 剪辑用户已有的讲课、访谈、实拍、录屏、音频或播客素材。
- 把网页生成的图形、字幕版式或动画与现有音视频素材装配成混合成品。
- 从一张全新二次元角色图或确认的角色设定开始，建立母版、连续校准视频、AI 视觉标注的普通话口型素材库，并把新文本或录音合成为可重复使用的口播形象视频。

不适用于：

- 通读课程、书籍、访谈或长材料后判断什么最值得分享；这属于内容研究或分享候选识别。
- 只交付能够脱离媒体独立成立的短帖、Thread 或短文案；这属于内容成文。
- 主动研究主题、补充事实证据、撰写文章或 Newsletter。
- 规划现场拍摄、器材、场景、人员或拍摄日程。
- 制作或编辑 PPTX、Keynote 等幻灯片文件；网页动画即使采用演示控制，也不改变文件边界。
- 账号定位、平台运营、排期、上传、发送或发布。

用户只点名本 Skill 并提供内容、但没有说明成品时，默认先给出一个首选载体及理由，完成可供确认的媒体文案后停止，不直接创建完整媒体。用户已经明确要求样稿或成品时，按对应授权继续。

## 开始前的七项判断

收到请求后先确定：

1. **内容真源**：允许使用的核心主张、支撑材料、必要语境、事实边界、来源位置和可用素材是否足以支撑成品；个人或系列内容是否同时带有已经确认的观点取向与表达方式。
2. **目标受众**：受众已经知道什么、看完或听完应当理解、感受或采取什么动作。
3. **信息职责与时间基准**：文字、旁白、画面、同期声和配套帖子分别讲清什么；时长由完整文案、真实语音、现有素材还是固定平台规格决定。
4. **交付物**：需要载体建议、媒体文案、风格样稿、构图样稿、动效或声音样稿，还是完整成品。
5. **现有素材**：已有文字、图片、品牌元素、录音、实拍、录屏、字幕、音乐和参考样本分别承担什么作用。
6. **授权层级**：当前是否允许写文案、创建网页、处理素材、调用外部模型、安装工具、导出文件或发布。
7. **风格来源**：当前项目是否已有确认的制作配方或风格档案，用户是否提供品牌与参考；都没有时由内容关系和载体自动选择哪个内置配方或中性方向。

用户已经说清时直接执行。缺失信息只影响局部时从现有内容采用保守且可逆的选择；缺口会改变大部分文案、载体、构图、剪辑或保存位置时，先读取材料和项目规则，仍无法确定再询问一个关键问题。

内容真源不完整时，只补齐媒体表达所需的结构，不自行发明事实、第一人称经历、来源或作者立场。需要外部研究或事实核查时，先说明缺口并取得对应授权；未获得授权时停在现有材料能够支持的范围。

个人或系列内容带有上游确认的 `editorial_position` 与 `voice_contract` 时分别读取：前者决定媒体文案强调什么、如何评价和说到哪里，后者决定视角、推进、句子节奏、幽默和结尾。两者缺失时沿当前内容真源与用户已确认文案制作，不从视觉参考、制作配方或平台模板推导作者观点和口吻。

同一来源准备派生多个成品，或者当前任务属于连续系列时，读取上游提供的来源标识、来源版本和内容单元标识。每个媒体项目保留这组来源信息，并另外记录项目标识、当前媒体文案版本和实际导出物；来源版本发生变化时，先核对受影响项目使用的主张、素材与文案，再继续导出。

## 默认正向流程

### 1. 选择具体载体

根据内容怎样成立和现有素材选择一个首选载体：

- 定义、静态关系、对比、框架、清单或一眼可见的结论，优先静态图文卡片、封面或图表。
- 状态变化、数值变化、循环、反馈、局部演示或需要原位逐步显现的关系，优先 GIF 或动态图解。
- 由代码画面承担的多场景推进、逐步揭示或现场讲解，优先网页动画；需要演讲者决定何时推进时使用手动播放，需要连续观看或导出时使用自动播放，需要页内自动运动但在章节边界等待时使用混合播放。
- 人物表达、故事、访谈、讲课、实拍、录屏，或多场景价值主要来自连续声音和镜头时，优先视频。
- 主要价值来自讲述、对谈、访谈、声音氛围或适合伴随收听的连续内容，优先音频或播客。

同一内容适合多个载体时，结合目标受众、已有素材、平台规格、制作成本和信息损失给出一个首选。只有备选能够提供不同的真实价值时才补充。

### 2. 先完成媒体文案

制作画面或声音前，先完成当前媒体成品需要的标题、开头、正文、口播、字幕、节目结构和配套帖子。媒体文案必须能说明核心主张、支撑和必要边界；画面与声音负责降低理解成本、建立节奏或增强体验，不承担修补空洞文案的任务。用户只要一份能够脱离媒体独立成立的短帖、Thread 或短文案时，交给 `100x-learning` 的内容成文流程；帖子与当前卡片、GIF、视频、音频或播客共同完成表达时，继续留在本流程。

先写清各通道的信息职责。旁白承担主要解释时，单独听旁白应当能够知道正在讲什么、关键机制怎样成立以及结论是什么；画面再负责展示空间关系、状态变化、数量和情绪。视觉主导的作品可以让旁白更少，但制作说明必须指出哪些理解只能从画面获得。媒体时长服从完整表达；风格、构图或动效样稿只验证对应层面，不反过来截短已经确认的内容。

媒体文案默认由当前模型起草和修改。用户明确点名 DeepSeek 或其它写作生产者时才切换，并服从该入口的可用性与失败边界。逐字引用、数据标签、来源名称和用户确认保持原样的文字直接传递。

个人或系列内容的媒体文案先按 `editorial_position` 完成信息选择和评价方向，再按 `voice_contract` 完成实际措辞；当前作品的时长、开头任务、信息密度和情绪强度继续由成品职责决定。修改其中一层时保留其它已经确认的输入。

用户已经确认文案时不重新改写。用户只要求文案时，交付文案后停止；用户要求样稿或完整制作时再进入下一步。

### 3. 建立语义片段和时间结构

时间型媒体在制作前把已确认文案拆成连续语义片段。每段记录当前新增的信息、旁白或同期声范围、画面职责、运动类型、进入和退出方式，以及时长由文本估算、真实语音、原始素材还是固定规格决定。代码生成的时间型网页把这些片段保存为有序场景；演示中的“页”也是场景，页内逐步出现保存为步骤。手动、自动和混合播放只改变时间由谁推进，不复制内容、布局或动画。生成或录制声音后，用实际音频和时间标记更新片段边界，再让场景时间线与之对齐。

运动按当前含义选择：持续现象使用片段内循环；从杂乱走向有序、从弱到强等方向性变化使用一次演进并在结果状态停留或循环；反馈关系使用保留累积状态的局部循环；比较保持可同时读取的状态；概念切换使用承接前后对象的转场；结论允许稳定停留。需要延长画面时，延长当前语义片段自己的运动或停留，旁白进入下一段时画面也进入对应片段。只有成品本身就是无推进叙事的无缝循环时，完整时间线才作为一个循环单元。

固定时长存在时，先在内容层调整取舍和表述，再生成或更新声音与画面。轻微语速调整可以用于消除小幅时间差；会损害可懂度的压缩改为修订文案或视觉结构。样稿通过后，完整成品从全量媒体文案和语义片段扩展，不把代表性样稿重复成整片。

### 4. 确定制作真源

每类成品只保留一个活动制作入口：

- **网页真源**：所有代码生成的图文卡片、封面、图表、概念图、GIF、动态图解、排版动画、可手动推进的多场景 HTML 和无实拍视觉画面。图文卡片也必须先在网页中完成；静态图片、交互演示和连续视频只是同一网页场景的不同播放或导出结果。
- **视频时间线**：用户已有的实拍、讲课、访谈、录屏或多场景视频，以及需要声音装配的最终视频。
- **音频时间线**：录音、访谈、旁白和播客。
- **混合项目**：网页继续负责代码生成的图形、字幕版式和动画，视频或音频时间线负责素材选择、声音和最终装配；后期项目不重新绘制网页负责的内容。

代码生成视觉需要进入结构化媒体编辑器时，网页包继续保存结构和动画，编辑器项目只保存该片段的文字、布局、样式和时间覆盖值。人工调整和后续 AI 调整都通过编辑器公开的同一种片段状态操作完成；只有增加组件、改变结构或重写复杂动画时才修改网页包本身。

内容、样式、布局、时间线和输出设置分别集中保存。用户反馈后修改对应真源，再重新预览和导出；不把截图、GIF、音频或视频成品变成第二个编辑入口。

### 5. 选择并继承制作配方

用户不需要先学会或反复描述视觉风格。按以下顺序确定当前视觉与动效语言：同一项目已经确认的制作配方或风格档案；用户提供的品牌规范或参考样本；`references/visual-production-profiles.md` 中与内容、载体和素材相符的内置配方；都不适用时，由当前内容关系形成一个保守且可修改的方向。除非两个合理方向会让受众理解、品牌气质或制作成本明显不同，否则直接选择一个并用一句话说明，不把风格命名问题抛给用户。

制作配方只决定视觉与动效怎样表达。内容取舍读取 `editorial_position`，文字和口播方式读取 `voice_contract`，构图从当前内容关系产生。首次确认后，把实际采用的字体、色彩、图形、材质、图片处理、密度和动效规则集中保存为当前项目的风格档案；同一系列后续成品默认继承，只有内容不适用、用户要求变化或目标平台造成实质冲突时才调整，并说明变化范围。

### 6. 在最早未确认的层面做样稿

- **文案确认**：确认主张、推进、语气和配套文字职责。
- **风格样稿**：只验证字体、线条、颜色、纹理、图标、光影、密度和留白。
- **构图样稿**：验证完整内容骨架、层级、容量、字幕安全区和持续可见的上下文。
- **动效或声音样稿**：只实现一个代表性变化、转场、短循环、旁白段落或声音处理。
- **完整成品**：沿已经确认的文案和样稿扩展，不重新发明结构或风格。

时间型样稿同时标明它代表哪个语义片段、验证哪种运动或声音处理，以及完整制作时哪些其它片段仍需各自设计。样稿时长只覆盖该验证目标；完整成品的时长继续由全量文案、真实声音和素材决定。

用户说“先做一个小样”时，根据当前上下文判断要验证哪一层，并在制作前用一句话说明本次样稿负责什么。这个差异会改变大部分结果且无法从上下文判断时，只询问这一项。样稿未确认时停在样稿；用户明确授权一次完成时可以连续完成各层并直接验收成品。

确认点由错误被放大的范围、返工成本和外部费用决定，不固定要求所有任务经过相同阶段。进入付费模型调用、长时间批量制作、大规模转码或会让前序判断扩散到大量成品的步骤前，先在最早未确认层交付足以判断方向的最小样稿并等待确认；用户已经看过等价样稿，或明确授权按当前方向一次完成时，可以继续执行而不重复暂停。

批量制作时，每个条目独立记录当前确认层。用户只确认部分条目时，只推进已经确认的条目；未确认或要求修改的条目停留在当前层，不阻塞其它条目，也不让已经通过的条目退回重做。修改共同文案、共享样式、布局规则或其它会影响已通过条目的上游真源时，先说明影响范围，再更新受影响的条目。

参考样本按内容、信息结构、视觉或声音风格、运动或剪辑方式、平台包装分层理解。用户只要求参考某种风格时，只提炼该层，不复制样本的流程、布局、观点、经历或固定场景。

### 7. 制作、检查与交付

制作时只使用用户提供或明确允许获取的素材。外部、用户提供和生成素材进入项目时使用唯一的 v2 `media-sources.json` 保存文件哈希、取得方式、权利、生成过程和声音身份；素材导入与时间线采用是两个独立动作。外部素材搜索、付费素材、模型调用、依赖安装和大规模转码分别需要当前任务的明确授权。复制或公开使用媒体前核对许可证、署名和用户提供的使用边界。

先检查活动真源能够打开、播放和修改，再在真实浏览器或媒体播放器中检查关键状态，最后检查用户点名的导出结果。技术检查至少覆盖文字与字幕、画面裁切、层级、时间、音画同步、声音可懂度、编码、尺寸、时长和文件可用性；审美检查回到用户确认的风格层和内容任务。

默认交付顺序：先说明采用的载体和结果，再提供媒体文案、可编辑真源、预览或样稿、用户点名的导出文件，以及会影响继续使用的必要规格。用户只要求其中一层时只交付该层。

## 任务路由

只读取完成当前任务需要的 reference：

| 任务 | 读取资源 | 读取目的 |
| --- | --- | --- |
| 选择载体、起草媒体文案、分镜或节目结构 | `references/content-to-media.md` | 确定内容真源、媒体职责和文案结构 |
| 自动选择或继承视觉与动效配方 | `references/visual-production-profiles.md` | 按内容与载体选择完整制作方法，并保存项目级风格档案 |
| 图文卡片、封面、图表、GIF、代码动画、可手动推进的多场景 HTML 或无实拍视觉 | `references/web-visual-production.md` | 建立网页真源、场景合同、播放方式、样稿、确定性时间线和派生媒体 |
| 社交平台卡片、封面、轮播图或以静态卡为基础的短动态图 | `references/social-card-production.md` | 完成逐页职责、版式容量、多比例重新构图、缩略图与真实画布验收 |
| 网页视觉中使用照片、截图、产品图、生成图或视频帧 | `references/visual-asset-placement.md` | 确认素材来源，按场景槽位处理主体、文字安全区、截图完整性和各输出变体裁切 |
| 视频、音频或播客实际进入素材导入、片段选择和分级交付 | `references/media-project-contracts.md` | 使用唯一素材账本、真实区间门槛和机器可读交付报告 |
| 把网页素材交给 MediaFlow Pro 或其它结构化媒体编辑器继续精调 | `references/structured-media-editor-cli.md` | 查询自描述能力，导入通用网页包并沿同一片段状态读写 |
| 剪辑已有实拍、讲课、访谈、录屏或混合视频 | `references/video-post-production.md` | 建立视频时间线并完成画面、字幕和声音装配 |
| 从单张图或角色设定建立、检查或使用无需 Live2D 的二次元中文口播形象 | `references/anime-avatar-production.md` | 从母版与校准视频建立 AI 视觉口型库，规范化语音时间轴，整帧合成；按用户要求再加入固定圆形或方形角色窗 |
| 用户明确要求 Live Photo | `references/live-photo-delivery.md` | 用同一静态构图生成配对文件，检查元数据、`.pvt`、联系表和设备导入 |
| 编辑录音、访谈、音频或播客 | `references/audio-podcast-production.md` | 建立音频时间线并完成清理、结构、混音和附属产物 |
| 用户要求合成旁白、点名 EdgeTTS 或需要为视频、动画、播客生成语音 | `references/speech-synthesis.md` | 选择声音入口，派生合成文本，生成原始语音和时间信息，并按真实时长交给媒体时间线 |
| 实际导出或交付媒体 | `references/review-and-export.md` | 沿真实链路检查源文件、预览、编码和成品 |

代码生成视觉同时读取 `references/content-to-media.md`、`references/visual-production-profiles.md` 和 `references/web-visual-production.md`。已有项目风格档案时先继承，再检查当前内容是否仍适用；没有时由制作配方 reference 自动选择。成品属于社交卡、平台封面、轮播图或静态卡式短动态图时再读取 `references/social-card-production.md`；实际使用照片、截图、生成图或视频帧时读取 `references/visual-asset-placement.md`。视频、音频或播客实际进入文件制作时读取 `references/media-project-contracts.md`，从 `assets/media-project-starter/` 建立项目合同；带声音的无实拍视频再读取 `references/video-post-production.md`。从单张图或角色设定建立二次元中文口播形象、检查校准视频、标注视觉口型、复用素材库或把它合成进视频时读取 `references/anime-avatar-production.md`，同时读取 `references/speech-synthesis.md`、`references/video-post-production.md` 和 `references/review-and-export.md` 中当前阶段实际需要的部分。长视频、录屏或关键区间不明确时先用 `scripts/make-video-contact-sheet.py` 检查稀疏代表帧，再用 `scripts/validate-clip-selections.mjs` 检查真实选段。需要生成声音时同时读取 `references/speech-synthesis.md`；播客封面或音频可视化只让网页负责视觉部分。用户明确要求 Live Photo 时才读取 `references/live-photo-delivery.md`。实际生成文件时总是读取 `references/review-and-export.md`。

制作个人网页卡片时同时读取 `assets/creator-identity/identity.json`，把其中的头像文件和昵称作为当前项目明确采用的个人身份资源，并在不遮挡主内容的位置完成署名；当前请求提供另一套作者身份时使用用户这次提供的身份。来源项目、赞助者、Skill 元数据、风格参考和案例中的作者信息只证明素材来源或示范署名结构，不成为当前作品作者。内容是项目、工具或资源等重复字段清单时，再读取 `assets/web-card-cases/catalog.json`，选择匹配案例并查看它的预览、网页、清单和风格档案；案例提供结构与制作链参考，当前内容继续决定字段、密度、配色和构图。

用户要求在 MediaFlow Pro 中继续混剪、配音、加字幕或手动精调网页图层时，再读取 `references/structured-media-editor-cli.md`。先调用 CLI 的 `describe`，只使用它实际声明的能力；不要根据产品名称猜测内部表、字段、文件布局或未声明命令。

## 工具与动作边界

- 默认允许读取用户提供的本地内容与素材，并在用户要求制作时写入当前任务的明确输出位置。
- 输出位置未说明但当前项目已有约定时遵循项目约定；没有可推断位置且文件交付是主要结果时再询问。
- 代码生成视觉优先复制 `assets/web-media-starter/` 整个目录作为中性起点；`editable-media.json` 是场景、步骤、播放方式、内容版式合同、输出比例、默认数据、主题、资源和量化质量规则的权威配置，HTML/CSS 负责结构和渲染算法，通用运行时负责场景切换、时间推进、编辑状态和导航。starter 不决定当前内容的结构或风格。
- 实际制作网页时运行 `scripts/validate-editable-media.mjs` 检查用户要求的每个“输出比例 × 场景”，并查看脚本从同一网页真源生成的截图。校验器只测结构、播放链路和已声明规则，不能代替人工检查内容、素材与风格。
- 独立素材使用 `scripts/import-media-asset.mjs` 进入 v2 素材账本，再由活动网页清单、视频时间线或音频项目显式采用；运行 `scripts/validate-media-sources.mjs` 证明真实文件、字节数和哈希与记录一致。
- 长视频、录屏或关键区间不明确时使用 `scripts/make-video-contact-sheet.py`；它只读取真实素材并生成诊断联系表，不成为新的剪辑真源。人物表达的选段另用 `scripts/validate-clip-selections.mjs` 检查真实时长、重复和已经完成的语义边界审核，联系表不能代替试听。
- 新二次元口播角色先用 `scripts/anime-avatar-project.py` 的 `init` 命令建立项目；校准视频只由脚本抽帧和生成索引联系表，A/I/U/E/O/CLOSED、相对强度和连续动作区间必须由 Agent 直接看画面写入 `visual-viseme-library.json`。代码不得用像素、轮廓、聚类或关键点替代这一步视觉判断。
- 视觉口型库通过 `scripts/anime-avatar-project.py` 的 `validate-library` 命令和反向审查图后，`scripts/render-anime-avatar.py` 才能读取已确认的 `speech-timeline.json` 合成整帧口播形象。新台词复用现有库，不重新识图或手工指定大张口帧。
- 用户要求右下角等固定位置的圆形或方形角色窗时，用 `scripts/compose-anime-avatar-inset.py` 建立并渲染 `schemas/anime-avatar-inset.v1.schema.json` 任务。Agent 从代表帧确定一次正方形裁切、窗口坐标和尺寸，所有帧复用同一组值；角色窗默认持续到底片结束，角色轨必须覆盖完整时长。有台词的区间按音素时间轴驱动口型，前置静音、句中停顿和实际存在的尾静音统一使用素材库中的连续闭嘴动态；不固定追加尾段，只有用户明确要求时才隐藏。成品联系表和完整播放通过后再进入常规媒体交付。
- 新文本使用已授权且当前可用的 EdgeTTS 时，`scripts/synthesize-avatar-speech.py` 同时保存原始语音、供应方 WordBoundary 和待试听的逐字时间轴；只有人工或 Agent 听音确认并把 `timing.reviewed` 改为 `true` 后，渲染器才接受。
- 视频或音频导出后使用 `scripts/verify-media-delivery.py` 生成交付报告；`technical_ready`、人工完整审看、权利复核和 `delivery_ready` 分开判断，正式交付不得只凭机器报告自动通过。
- Chrome、Node、Python、FFmpeg、FFprobe 或媒体编辑软件只有完成当前产物确实需要时才使用。缺少依赖时先报告具体能力缺口，不自动安装，也不另写一套与制作真源不一致的实现。
- 用户明确要求合成语音或点名 EdgeTTS 等声音入口后，可以调用当前环境已经可用的对应服务生成声音；安装新工具、克隆特定真人声音和获取额外付费服务继续按各自动作边界确认。
- 用户只要求建议、文案、分镜或剪辑方案时不创建网页、不处理素材、不导出媒体。
- 账号操作、上传、发送和发布始终需要单独授权；完成媒体文件不等于获得发布权限。

## 停止条件

- 只要载体建议：首选载体、理由、所需素材和下一步已经清楚时停止。
- 只要媒体文案：文案能够独立成立并符合内容真源、受众和载体时停止。
- 只要样稿：用户点名的样稿层已经在正确真源中可见、可播放或可听时停止。
- 要完整媒体：活动真源可修改，网页清单和实际页面一致，指定成品真实生成、被最终工具读取并通过一致性检查后停止。多场景网页必须证明场景和步骤确实切换，手动、自动或混合模式按当前交付工作；多比例项目必须逐个检查用户要求的输出变体；正式视频或音频必须由 `final` 交付报告分别证明技术就绪、人工完整审看和权利复核；Live Photo 必须按其 reference 到达实际授权范围内的完整验证层级。
- 要完整二次元口播形象：用户确认的母版、固定机位校准视频、AI 视觉标注并通过质量门的可复用素材库、已确认语音时间轴和真实合成视频全部存在；完整观看口型、动作、晚段重复、切换、圆形或方形裁切和最终底片后停止。
- 要修改：修改活动真源并更新用户点名的派生格式后停止，不扩张成其它比例、平台版本或发布方案。
- 用户要求停止或否定方向时立即停止制作，说明当前保留项和最早未确认层；未获继续授权不生成替代版本。

## 交付前检查

- 内容真源是否足以支撑媒体，没有补写未经确认的事实、经历或立场？
- 个人或系列内容是否分别落实已确认的观点取向与表达方式，视觉制作配方是否只承担画面与动效职责？
- 系列派生成品是否能够回到准确的来源版本、内容单元和媒体文案版本？
- 首选载体是否由内容关系和素材决定，而非固定偏好某一种形式？
- 是否先完成媒体文案，再决定视觉、声音、分镜和剪辑？
- 社交卡、封面和轮播图是否有清楚的逐页职责、版式容量和缩略图检查，多比例版本是否分别重新构图？
- 使用照片、截图、生成图、声音或视频帧时，是否通过 v2 素材账本保存真实文件哈希、来源、授权、生成过程、声音身份、主体位置和各输出变体裁切，并由消费者显式采用？
- 时间型媒体是否已经按语义片段分配旁白、画面和运动职责，代码网页是否把它们落实为场景和步骤，最终时长是否来自完整内容、真实声音或明确规格？
- 多场景 HTML 是否仍使用同一网页动画真源，播放方式是否只控制时间推进，没有复制成另一套演示内容或运行时？
- 循环是否发生在当前语义片段内部，方向性变化是否保留演进和结果状态，画面是否随旁白推进？
- 图文卡片及其它代码生成视觉是否确实以网页为可编辑真源？
- 网页清单是否真实提供场景、步骤、播放方式、内容版式合同、输出变体、默认数据、主题、资源和质量规则，HTML 是否已经移除同名重复配置？
- 用户要求的每个“输出变体 × 场景”是否通过 `scripts/validate-editable-media.mjs`；长或不明确的视频素材是否通过真实联系表选择区间，并另行完成真实区间、去重和完整语义审核？
- GIF 是否用动画解释变化；内容不属于流程时是否避免自动做成流程图？
- 已有实拍是否从后期时间线开始，没有扩张成拍摄和制片计划？
- 合成语音是否保留已确认文案、合成文本哈希、供应方声音 ID、声音名称、生成参数、原始音频哈希和实际时长，精确声音缺失时是否停止而非静默换人？
- 二次元口播形象是否来自一张已确认母版和同一母版的连续固定机位校准视频；角色素材库是否由 AI 直接视觉标注且每类元音至少有两条完整 take，而不是独立嘴图、独立整图或代码推断分类？
- 新语音是否使用准确逐字稿、明确静音裁切和已复核时间轴；渲染是否只在音节核心硬匹配并保持周围连续运动，没有长时间大张口、快速无意义开合、晚段机械重复或人物整体跳变？
- 风格、构图、动效和声音样稿是否各自只验证对应层面？
- 是否优先继承已有项目风格；没有用户参考时是否由内容自动选择了合适配方，而没有要求用户从头描述风格？
- 用户只要小样时是否停在小样，用户要成品时是否检查了真实源文件到导出结果的链路？
- 正式视频或音频是否存在 `final` 交付合同和报告，机器技术检查、人工完整审看、素材权利是否分别通过？
- 外部素材、模型、安装、上传和发布是否都处于当前授权范围？
- 交付是否包含用户真正需要的可编辑源和指定成品，没有附送额外平台版本？
