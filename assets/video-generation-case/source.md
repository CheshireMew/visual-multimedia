# 可恢复的媒体生成

外部媒体生成最容易出错的地方，不是提示词，而是一次远程调用跨过了本地进程边界。请求可能已经计费并进入队列，本地却因为网络超时没有收到任务编号。如果这时直接重试，用户可能为同一个画面付两次钱。

可靠流程先把当前内容版本、规范化输入、费用预估和批准证据保存下来，再生成稳定幂等键。提交后只查询同一个远程任务。文件下载并校验成功以前，它仍然是生成任务，不是可用素材。

最终文件进入素材账本之后，时间线还要显式采用对应的 source id。这样可以区分“供应方已经生成”“项目已经可靠保存”和“成片确实使用”三个不同事实。
