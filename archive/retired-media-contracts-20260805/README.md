# Retired media-delivery v2 contract

`media-delivery.v2.schema.json` is retained only as migration history. Active producers, examples, validators and documentation use `schemas/media-delivery.v3.schema.json`; runtime code does not load this archived schema and does not keep a v2 compatibility path.

The archived file is byte-for-byte identical to the former active schema at the time it was retired.
