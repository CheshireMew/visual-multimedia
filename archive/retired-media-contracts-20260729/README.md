# 已退役媒体合同

这里保存 2026-07-29 一次性迁移前的只读 schema，原因是项目规则禁止直接删除文件。

这些文件不属于活动架构，不得被 starter、脚本、reference、案例或新项目引用：

- `media-sources.v2.schema.json`
- `clip-selections.v1.schema.json`
- `media-delivery.v1.schema.json`

活动合同只位于 `schemas/`，以 v3 素材账本及配套的转写、选段、项目状态、审阅和 v2 交付合同为准。没有兼容读取器或旧版恢复逻辑。
