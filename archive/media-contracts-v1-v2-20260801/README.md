# 媒体状态与评审旧合同归档

这里仅保留 `media-project-state` v1、`media-review` v1 和 v2 的历史 schema，供人工追溯。生产脚本、校验器、starter、案例和文档都不读取此目录；当前活动合同是 `schemas/media-project-state.v2.schema.json` 与 `schemas/media-review.v3.schema.json`。
