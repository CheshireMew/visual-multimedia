# Retired media contract

`media-sources.v2.schema.json` is retained only as migration history. Active projects and validators use `schemas/media-sources.v3.schema.json`; runtime code does not load this archived schema.
