# Interview explainer v1 retired contract

This directory preserves the retired v1 draft and plan schemas for historical inspection only. The active producer uses `interview-explainer` profile 1.4.0 with draft v2 and plan v2. Runtime code, catalogs, validation and new projects do not read these files.
